/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PlantillaMensaje } from '../../models/plantilla-mensaje';
import { PlantillaMensajeData } from '../../models/plantilla-mensaje-data';

export interface ActualizarPlantillaPlantillasPlantillaIdPut$Params {
  plantilla_id: number;
      body: PlantillaMensajeData
}

export function actualizarPlantillaPlantillasPlantillaIdPut(http: HttpClient, rootUrl: string, params: ActualizarPlantillaPlantillasPlantillaIdPut$Params, context?: HttpContext): Observable<StrictHttpResponse<PlantillaMensaje>> {
  const rb = new RequestBuilder(rootUrl, actualizarPlantillaPlantillasPlantillaIdPut.PATH, 'put');
  if (params) {
    rb.path('plantilla_id', params.plantilla_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PlantillaMensaje>;
    })
  );
}

actualizarPlantillaPlantillasPlantillaIdPut.PATH = '/plantillas/{plantilla_id}';
