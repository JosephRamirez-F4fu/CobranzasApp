/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Campania } from '../../models/campania';
import { CampaniaData } from '../../models/campania-data';

export interface ActualizarCampaniaCampaniasCampaniaIdPut$Params {
  campania_id: number;
      body: CampaniaData
}

export function actualizarCampaniaCampaniasCampaniaIdPut(http: HttpClient, rootUrl: string, params: ActualizarCampaniaCampaniasCampaniaIdPut$Params, context?: HttpContext): Observable<StrictHttpResponse<Campania>> {
  const rb = new RequestBuilder(rootUrl, actualizarCampaniaCampaniasCampaniaIdPut.PATH, 'put');
  if (params) {
    rb.path('campania_id', params.campania_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Campania>;
    })
  );
}

actualizarCampaniaCampaniasCampaniaIdPut.PATH = '/campanias/{campania_id}';
