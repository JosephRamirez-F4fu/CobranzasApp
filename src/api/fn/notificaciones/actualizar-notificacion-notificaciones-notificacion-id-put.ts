/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Notificacion } from '../../models/notificacion';
import { NotificacionData } from '../../models/notificacion-data';

export interface ActualizarNotificacionNotificacionesNotificacionIdPut$Params {
  notificacion_id: number;
      body: NotificacionData
}

export function actualizarNotificacionNotificacionesNotificacionIdPut(http: HttpClient, rootUrl: string, params: ActualizarNotificacionNotificacionesNotificacionIdPut$Params, context?: HttpContext): Observable<StrictHttpResponse<Notificacion>> {
  const rb = new RequestBuilder(rootUrl, actualizarNotificacionNotificacionesNotificacionIdPut.PATH, 'put');
  if (params) {
    rb.path('notificacion_id', params.notificacion_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Notificacion>;
    })
  );
}

actualizarNotificacionNotificacionesNotificacionIdPut.PATH = '/notificaciones/{notificacion_id}';
