/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginationDataTiposEnvio } from '../../models/pagination-data-tipos-envio';

export interface ListarTiposEnvioTiposEnvioGet$Params {
  skip?: number;
  limit?: number;
}

export function listarTiposEnvioTiposEnvioGet(http: HttpClient, rootUrl: string, params?: ListarTiposEnvioTiposEnvioGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginationDataTiposEnvio>> {
  const rb = new RequestBuilder(rootUrl, listarTiposEnvioTiposEnvioGet.PATH, 'get');
  if (params) {
    rb.query('skip', params.skip, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginationDataTiposEnvio>;
    })
  );
}

listarTiposEnvioTiposEnvioGet.PATH = '/tipos-envio/';
