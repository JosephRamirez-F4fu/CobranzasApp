/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Calendario } from '../../models/calendario';
import { CalendarioData } from '../../models/calendario-data';

export interface CrearCalendarioCalendariosPost$Params {
      body: CalendarioData
}

export function crearCalendarioCalendariosPost(http: HttpClient, rootUrl: string, params: CrearCalendarioCalendariosPost$Params, context?: HttpContext): Observable<StrictHttpResponse<Calendario>> {
  const rb = new RequestBuilder(rootUrl, crearCalendarioCalendariosPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Calendario>;
    })
  );
}

crearCalendarioCalendariosPost.PATH = '/calendarios/';
