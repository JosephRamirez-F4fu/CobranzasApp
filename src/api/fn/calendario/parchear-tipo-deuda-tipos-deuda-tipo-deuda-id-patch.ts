/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TipoDeuda } from '../../models/tipo-deuda';
import { TipoDeudaPatch } from '../../models/tipo-deuda-patch';

export interface ParchearTipoDeudaTiposDeudaTipoDeudaIdPatch$Params {
  tipo_deuda_id: number;
      body: TipoDeudaPatch
}

export function parchearTipoDeudaTiposDeudaTipoDeudaIdPatch(http: HttpClient, rootUrl: string, params: ParchearTipoDeudaTiposDeudaTipoDeudaIdPatch$Params, context?: HttpContext): Observable<StrictHttpResponse<TipoDeuda>> {
  const rb = new RequestBuilder(rootUrl, parchearTipoDeudaTiposDeudaTipoDeudaIdPatch.PATH, 'patch');
  if (params) {
    rb.path('tipo_deuda_id', params.tipo_deuda_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TipoDeuda>;
    })
  );
}

parchearTipoDeudaTiposDeudaTipoDeudaIdPatch.PATH = '/tipos-deuda/{tipo_deuda_id}';
