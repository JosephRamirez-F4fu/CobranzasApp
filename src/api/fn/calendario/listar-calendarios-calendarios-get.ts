/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginationDataCalendarios } from '../../models/pagination-data-calendarios';

export interface ListarCalendariosCalendariosGet$Params {
  skip?: number;
  limit?: number;
}

export function listarCalendariosCalendariosGet(http: HttpClient, rootUrl: string, params?: ListarCalendariosCalendariosGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginationDataCalendarios>> {
  const rb = new RequestBuilder(rootUrl, listarCalendariosCalendariosGet.PATH, 'get');
  if (params) {
    rb.query('skip', params.skip, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginationDataCalendarios>;
    })
  );
}

listarCalendariosCalendariosGet.PATH = '/calendarios/';
