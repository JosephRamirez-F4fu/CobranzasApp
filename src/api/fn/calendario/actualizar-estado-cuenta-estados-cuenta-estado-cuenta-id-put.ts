/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EstadoCuenta } from '../../models/estado-cuenta';
import { EstadoCuentaData } from '../../models/estado-cuenta-data';

export interface ActualizarEstadoCuentaEstadosCuentaEstadoCuentaIdPut$Params {
  estado_cuenta_id: number;
      body: EstadoCuentaData
}

export function actualizarEstadoCuentaEstadosCuentaEstadoCuentaIdPut(http: HttpClient, rootUrl: string, params: ActualizarEstadoCuentaEstadosCuentaEstadoCuentaIdPut$Params, context?: HttpContext): Observable<StrictHttpResponse<EstadoCuenta>> {
  const rb = new RequestBuilder(rootUrl, actualizarEstadoCuentaEstadosCuentaEstadoCuentaIdPut.PATH, 'put');
  if (params) {
    rb.path('estado_cuenta_id', params.estado_cuenta_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EstadoCuenta>;
    })
  );
}

actualizarEstadoCuentaEstadosCuentaEstadoCuentaIdPut.PATH = '/estados-cuenta/{estado_cuenta_id}';
