/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginationDataTiposEvento } from '../../models/pagination-data-tipos-evento';

export interface ListarTiposEventoTiposEventoGet$Params {
  skip?: number;
  limit?: number;
}

export function listarTiposEventoTiposEventoGet(http: HttpClient, rootUrl: string, params?: ListarTiposEventoTiposEventoGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginationDataTiposEvento>> {
  const rb = new RequestBuilder(rootUrl, listarTiposEventoTiposEventoGet.PATH, 'get');
  if (params) {
    rb.query('skip', params.skip, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginationDataTiposEvento>;
    })
  );
}

listarTiposEventoTiposEventoGet.PATH = '/tipos-evento/';
