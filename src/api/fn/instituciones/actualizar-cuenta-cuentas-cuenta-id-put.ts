/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Cuenta } from '../../models/cuenta';
import { CuentaData } from '../../models/cuenta-data';

export interface ActualizarCuentaCuentasCuentaIdPut$Params {
  cuenta_id: number;
      body: CuentaData
}

export function actualizarCuentaCuentasCuentaIdPut(http: HttpClient, rootUrl: string, params: ActualizarCuentaCuentasCuentaIdPut$Params, context?: HttpContext): Observable<StrictHttpResponse<Cuenta>> {
  const rb = new RequestBuilder(rootUrl, actualizarCuentaCuentasCuentaIdPut.PATH, 'put');
  if (params) {
    rb.path('cuenta_id', params.cuenta_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Cuenta>;
    })
  );
}

actualizarCuentaCuentasCuentaIdPut.PATH = '/cuentas/{cuenta_id}';
