/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Perfil } from '../../models/perfil';
import { PerfilData } from '../../models/perfil-data';

export interface ActualizarPerfilPerfilesPerfilIdPut$Params {
  perfil_id: number;
      body: PerfilData
}

export function actualizarPerfilPerfilesPerfilIdPut(http: HttpClient, rootUrl: string, params: ActualizarPerfilPerfilesPerfilIdPut$Params, context?: HttpContext): Observable<StrictHttpResponse<Perfil>> {
  const rb = new RequestBuilder(rootUrl, actualizarPerfilPerfilesPerfilIdPut.PATH, 'put');
  if (params) {
    rb.path('perfil_id', params.perfil_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Perfil>;
    })
  );
}

actualizarPerfilPerfilesPerfilIdPut.PATH = '/perfiles/{perfil_id}';
