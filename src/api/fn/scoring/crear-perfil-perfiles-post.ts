/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Perfil } from '../../models/perfil';
import { PerfilData } from '../../models/perfil-data';

export interface CrearPerfilPerfilesPost$Params {
      body: PerfilData
}

export function crearPerfilPerfilesPost(http: HttpClient, rootUrl: string, params: CrearPerfilPerfilesPost$Params, context?: HttpContext): Observable<StrictHttpResponse<Perfil>> {
  const rb = new RequestBuilder(rootUrl, crearPerfilPerfilesPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Perfil>;
    })
  );
}

crearPerfilPerfilesPost.PATH = '/perfiles/';
