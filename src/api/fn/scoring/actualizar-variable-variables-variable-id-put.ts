/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Variable } from '../../models/variable';
import { VariableData } from '../../models/variable-data';

export interface ActualizarVariableVariablesVariableIdPut$Params {
  variable_id: number;
      body: VariableData
}

export function actualizarVariableVariablesVariableIdPut(http: HttpClient, rootUrl: string, params: ActualizarVariableVariablesVariableIdPut$Params, context?: HttpContext): Observable<StrictHttpResponse<Variable>> {
  const rb = new RequestBuilder(rootUrl, actualizarVariableVariablesVariableIdPut.PATH, 'put');
  if (params) {
    rb.path('variable_id', params.variable_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Variable>;
    })
  );
}

actualizarVariableVariablesVariableIdPut.PATH = '/variables/{variable_id}';
