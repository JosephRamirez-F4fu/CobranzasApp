/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LoginGestorRequest } from '../../models/login-gestor-request';

export interface LoginGestorAdminAuthGestorLoginPost$Params {
      body: LoginGestorRequest
}

export function loginGestorAdminAuthGestorLoginPost(http: HttpClient, rootUrl: string, params: LoginGestorAdminAuthGestorLoginPost$Params, context?: HttpContext): Observable<StrictHttpResponse<{
[key: string]: any;
}>> {
  const rb = new RequestBuilder(rootUrl, loginGestorAdminAuthGestorLoginPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      [key: string]: any;
      }>;
    })
  );
}

loginGestorAdminAuthGestorLoginPost.PATH = '/auth/gestor/login';
