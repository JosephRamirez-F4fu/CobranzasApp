/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseNotificacionConfigracionResponse } from '../../models/api-response-notificacion-configracion-response';
import { NotificacionConfigracionRegister } from '../../models/notificacion-configracion-register';

export interface ActualizarConfiguracionDeNotificacion$Params {
  code: string;
  medioEnvio: 'SMS' | 'WHATSAPP' | 'IVR' | 'EMAIL';
      body: NotificacionConfigracionRegister
}

export function actualizarConfiguracionDeNotificacion(http: HttpClient, rootUrl: string, params: ActualizarConfiguracionDeNotificacion$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseNotificacionConfigracionResponse>> {
  const rb = new RequestBuilder(rootUrl, actualizarConfiguracionDeNotificacion.PATH, 'put');
  if (params) {
    rb.path('code', params.code, {});
    rb.path('medioEnvio', params.medioEnvio, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseNotificacionConfigracionResponse>;
    })
  );
}

actualizarConfiguracionDeNotificacion.PATH = '/notificacion-configuracion/{code}/medio/{medioEnvio}';
