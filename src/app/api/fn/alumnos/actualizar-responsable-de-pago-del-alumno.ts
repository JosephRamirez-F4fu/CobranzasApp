/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AlumnoResponsableRequest } from '../../models/alumno-responsable-request';
import { ApiResponseString } from '../../models/api-response-string';

export interface ActualizarResponsableDePagoDelAlumno$Params {
  id: number;
      body: AlumnoResponsableRequest
}

export function actualizarResponsableDePagoDelAlumno(http: HttpClient, rootUrl: string, params: ActualizarResponsableDePagoDelAlumno$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseString>> {
  const rb = new RequestBuilder(rootUrl, actualizarResponsableDePagoDelAlumno.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseString>;
    })
  );
}

actualizarResponsableDePagoDelAlumno.PATH = '/student/tutor/{id}';
