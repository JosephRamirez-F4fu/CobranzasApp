/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseListNotificacionResponse } from '../../models/api-response-list-notificacion-response';

export interface RegistrarAutomatico1$Params {
  cuotaId: number;
}

export function registrarAutomatico1(http: HttpClient, rootUrl: string, params: RegistrarAutomatico1$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseListNotificacionResponse>> {
  const rb = new RequestBuilder(rootUrl, registrarAutomatico1.PATH, 'post');
  if (params) {
    rb.path('cuotaId', params.cuotaId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseListNotificacionResponse>;
    })
  );
}

registrarAutomatico1.PATH = '/notificaciones/cuota/{cuotaId}/automatico';
