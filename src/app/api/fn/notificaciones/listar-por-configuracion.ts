/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseListNotificacionResponse } from '../../models/api-response-list-notificacion-response';

export interface ListarPorConfiguracion$Params {
  configuracionId: number;
}

export function listarPorConfiguracion(http: HttpClient, rootUrl: string, params: ListarPorConfiguracion$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseListNotificacionResponse>> {
  const rb = new RequestBuilder(rootUrl, listarPorConfiguracion.PATH, 'get');
  if (params) {
    rb.path('configuracionId', params.configuracionId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseListNotificacionResponse>;
    })
  );
}

listarPorConfiguracion.PATH = '/notificaciones/configuracion/{configuracionId}';
